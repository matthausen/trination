import React from 'react';

const Title = (props) => (
    <div className="header container">
        <h1 className="title">TRINATION</h1>
        <img src="/football.png" alt="trination-logo" className="football"/>
    </div>
);

export default Title;